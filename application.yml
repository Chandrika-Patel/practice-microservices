 spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Chandrika-Patel/practice-microservices

eureka:
    client:
       register:
              with-eureka: true
              fetch-registry: true         
          
